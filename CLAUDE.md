## Agent skills

### Issue tracker

Issues and specs live as GitHub issues in `donmonty/bar-metrics`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` (not yet created) + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
